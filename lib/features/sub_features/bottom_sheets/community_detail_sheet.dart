import 'package:flutter/material.dart';

final class CommunityDetailSheet extends StatelessWidget {
  const CommunityDetailSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
